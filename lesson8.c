#include <stdio.h>
#include <math.h> // important

int main(){

    //Useful math functions

    int x = 45;
    //float x = 3.99;

    //x = sqrt(x); square root
    //x = pow(x, 4); power (exponents)
    //x = round(x); (round off)
    //x = ceil(x); least integer function
    //x = floor(x); running down
    //x = abs(x); absolute value
    //x = log(x); logarithm
    //x = sin(x); sine
    //x = cos(x); cosine
    //x = tan(x); tangent

    printf("%f", x);

    return 0;
}